# WhatsApp Cloud Adapter

Native WhatsApp Cloud API adapter for Integrator.

- Trigger: `incoming_message`
- Action: `sendMessage`

Connection setup uses access token + phone number ID (stored encrypted in credentials metadata/secret fields).
